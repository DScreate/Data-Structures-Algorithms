strings = ["a", "b", "c", "d"]
# Note: Python does not have built-in support for Arrays, but Python Lists can be used instead.


strings.append("honda") # add element to array
strings.pop(1) # removes element at index n
strings.remove("c") # removes element that has value of input
strings.count("d") # returns the number of elements with the specified value.
print(strings)
strings.append("toyota") # add element to array
strings.append("q") # add element to array
print(strings[1:3])
# https://wiki.python.org/moin/TimeComplexity